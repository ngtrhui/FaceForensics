absl-py>=0.4.0          # vẫn tương thích
astor==0.8.1            # 0.7.1 không tương thích với Python 3.10+
certifi>=2022.12.7      # bản mới hơn
cffi>=1.15.1            # cần >=1.15 cho Python 3.10+
cmake>=3.22.0           # phiên bản cũ có thể gây lỗi build dlib
dlib==19.24.0           # bản mới hơn, hỗ trợ Python 3.10+
face-recognition==1.3.0 # tương thích với dlib mới
face-recognition-models==0.3.0
ffmpy==0.3.1
gast==0.5.3             # bản cũ không tương thích với Python 3.10+
grpcio==1.43.0
h5py==3.7.0             # hỗ trợ tốt hơn cho numpy mới
keras==2.11.0           # tương thích với TF >= 2.11
Markdown>=3.3.7
mkl-fft==1.3.1
mkl-random==1.2.2
munch==2.5.0
numpy==1.23.5           # numpy 1.16 không tương thích với Python 3.10+
nvidia-ml-py3==7.352.0
olefile==0.46
opencv-python==4.7.0.72 # 3.4.1.15 không tương thích
pathlib; python_version < "3.4"  # bỏ nếu dùng Python >= 3.4 (có sẵn pathlib)
Pillow==9.4.0
pretrainedmodels==0.7.4 # vẫn dùng được
protobuf==3.20.3        # tương thích TF cũ, không lỗi C++ ABI
pycparser==2.21
scandir; python_version < "3.5"  # bỏ nếu dùng Python >= 3.5
six==1.16.0
termcolor==2.1.1
torch==1.12.1
torchvision==0.13.1
tqdm==4.64.1
